#include <iostream>
using namespace std;

int main(){
    // declaring variables
    int a, b, result;
    
    // process;
    a = 5;
    b = 2;
    a = a + 1;
    result = a = b;
    
    // print the result 
    cout << result;

    // Initialization in variables;

    // type identier = initial_value
    int d = 0;
    
    // type identier (initial_value)
    int e (0);

    return 0;
}